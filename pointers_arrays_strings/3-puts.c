#include "main.h"
/**
 * _puts - returns the length of a string
 *@s: character to check
 *Return: the length of a string
 */
void _puts(char *str)
{
	char *str = a;

	_puts (str);
	{
	}
	return (0);
}
