#include "main.h"
/**
 * _puts - returns the length of a string
 *@s: character to check
 *Return: the length of a string
 */
void _puts(char *str)
{
	int l = 0;

	for(l = 0 ; s[l] != '\0' ; l++)
	{
		_putchar(s[l]);
		l++:
	}
	_putchar('\n');
	return (0);
}
