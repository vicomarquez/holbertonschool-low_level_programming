#include "main.h"
/**
 * _strlen - returns the length of a string
 *@s: character to check
 *Return: the length of a string
 */
int _strlen(char *s)
{
	int l;

	for (l = 0; s[l] != '\0' ; l++)
	{
	}
	return (l);
}
