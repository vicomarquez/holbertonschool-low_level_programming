#include "main.h"
/**
 * int _islower(int c) - checks for lowercase character
 *
 * Return: 1 if c is lowercase
 * Return: 0 otherwise
 */

int _islower(int c)
{
	int c;

	if ( _islower 'c' );
	{
		return (1);
		
		if (else)
		return (0);
	}

}
