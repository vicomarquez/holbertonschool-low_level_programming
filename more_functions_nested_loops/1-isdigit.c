#include "main.h"
/**
 *_isdigit - checks for a digit
 *
 *@c: the character to check
 *
 *Return: 1 if c is upercase, 0 otherwise
*/

int _isdigit(int c)
{
	if (c = '0' ; c <= '9' ; c++)
		return (1);
	else
		return (0);
}
